%h3 新規 Cost 登録
- if @errors
  %ul
    - @errors.each do |error|
      %li.error= error

= render 'templates/show_messages'

= form_for @cost do |f|
  .form-group
    = f.label :description
    = f.text_field :description, placeholder: 'Short description', class: 'form-control'
  = f.submit '作成', class: 'btn btn-default'
